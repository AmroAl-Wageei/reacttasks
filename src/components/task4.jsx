import React from 'react';

const StyledComponent = () => {
  const style = {
    backgroundColor: "lightblue",
    padding: "20px",
    borderRadius: "5px",
  };

  return <div style={style}>Orange Coding Academy</div>;
};

export default StyledComponent;
